<h1>Post Index</h1>

<% posts.forEach(function(post) { %>
    <div>
        <h2><%= post.title %> </h2>
        <h4>To: <%= post.to %> </h4>
        <div><%= post.description %> </div>
        <div><a href="/posts/<%= post.id %> ">View More</a></div>
    </div>

<% }); %> 